<form method="post" action="{{ path('privatisation_delete', {'id': privatisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ privatisation.id) }}">
    <button class="btn">Delete</button>
</form>
