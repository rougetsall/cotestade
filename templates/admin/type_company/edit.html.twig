
{% extends 'admin.html.twig' %}

{% block title %}Éditer catégorie Company{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3">
        <h1>Éditer catégorie</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
            <a href="{{ path('admin_type_company') }}" class="btn btn-sm btn-primary mr-1">Retour</a>
            {{ include('admin/type_company/_delete_form.html.twig') }}
        </div>
    </div>

    {{ include('admin/type_company/_form.html.twig', {'button_label': 'Éditer'}) }}

{% endblock %}