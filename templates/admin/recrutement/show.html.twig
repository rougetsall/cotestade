
{% extends 'admin.html.twig' %}

{% block title %}Recrutement{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3">
        <h1>Recrutement</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
            <a href="{{ path('admin_recrutement') }}">Retour</a>
            <a href="{{ path('admin_recrutement_new') }}" class="btn btn-sm btn-outline-primary">Ajouter un recrutement</a>
            <a href="{{ path('admin_recrutement_edit', {'id': recrutement.id}) }}" class="btn btn-sm btn-outline-primary">Éditer</a>
            {{ include('admin/recrutement/_delete_form.html.twig') }}
        </div>
    </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ recrutement.titre }}</td>
            </tr>
            <tr>
                <th>Annonce</th>
                <td>{{ recrutement.annonce }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ recrutement.date ? recrutement.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>cootrdonne</th>
                <td>{{ recrutement.contact }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
