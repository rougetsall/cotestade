{% extends 'base.html.twig' %}

{% block title %}Recrutement{% endblock %}

{% block body %}
    <h1>Recrutement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recrutement.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ recrutement.titre }}</td>
            </tr>
            <tr>
                <th>Annonce</th>
                <td>{{ recrutement.annonce }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ recrutement.date ? recrutement.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recrutement_index') }}">back to list</a>

    <a href="{{ path('recrutement_edit', {'id': recrutement.id}) }}">edit</a>

    {{ include('recrutement/_delete_form.html.twig') }}
{% endblock %}
